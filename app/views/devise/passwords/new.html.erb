<div class="container my-5">
  <div class="row justify-content-center">
    <div class="col-md-9 col-lg-8">
      
      <%# 外側のカード風コンテナ（シャドウと角丸） %>
      <div class="card shadow-lg border-0 rounded-3 overflow-hidden">
        <div class="row g-0">
          
          <%# --- 左側の装飾パネル (グラデーション) --- %>
          <div class="col-md-6 d-flex align-items-center justify-content-center p-5 text-white text-center" style="background: linear-gradient(to bottom right, #ffc107, #dc3545);">
            <div>
              <h2 class="h3 fw-bold mb-3">パスワードをお忘れですか？</h2>
              <p class="small opacity-90">
                ご登録のメールアドレスを入力してください。パスワードリセット用のリンクをお送りします。
              </p>
            </div>
          </div>

          <%# --- 右側のフォームエリア --- %>
          <div class="col-md-6 p-4 p-md-5">
            <h2 class="h4 fw-bold text-center mb-4">
              リセットリンクの送信
            </h2>

            <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
              <%# フォーム上部にDeviseのエラーメッセージを表示 %>
              <%= render "devise/shared/error_messages", resource: resource %>

              <div class="mb-3">
                <%= f.label :email, "メールアドレス", class: "form-label fw-bold" %>
                <%= f.email_field :email, autofocus: true, autocomplete: "email", required: true,
                                  class: "form-control rounded-pill", placeholder: "メールアドレスを入力" %>
              </div>

              <div class="d-grid gap-2 mt-4">
                <%= f.submit "リセットリンクを送信",
                             class: "btn btn-danger btn-lg rounded-pill fw-bold" %>
              </div>
            <% end %>

            <%# ---------------------------------------------------- %>
            <%# Login/Signupリンクを「ログインに戻る」に統合 %>
            <%# ---------------------------------------------------- %>
            <div class="mt-4 text-center">
              <%= link_to "ログインに戻る", new_session_path(resource_name), class: "small text-decoration-none text-muted" %>
            </div>
            <%# ---------------------------------------------------- %>

          </div>
        </div>
      </div>
    </div>
  </div>
</div>