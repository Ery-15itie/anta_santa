<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Anta Santa" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Bootstrap 5 CDN %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" 
          rel="stylesheet" 
          xintegrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" 
          crossorigin="anonymous">
    
    <%# Bootstrap Icons CDN %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%# Tailwind CSS CDN (念のため保持) %>
    <script src="https://cdn.tailwindcss.com"></script>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <%# フラッシュメッセージ固定表示用のCSS %>
    <style>
      .flash-messages-fixed-top {
        position: fixed;
        top: 20px; /* 画面最上部からの距離 */
        left: 50%;
        transform: translateX(-50%);
        z-index: 2000; /* 最前面に表示 */
        width: 90%;
        max-width: 600px; /* PCでの幅を制限 */
      }
      /* 淡い暖色系の背景を適用 */
      body {
        background-color: #FEF3E7;
      }
    </style>
    
  </head>

  <body>

    <%# ============== フラッシュメッセージ表示エリア（固定位置）=============== %>
    <div class="flash-messages-fixed-top">
      <%# notice/alert ロジック %>
      <% if notice %>
        <div class="alert alert-success alert-dismissible fade show text-center" role="alert">
          <%= notice %>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
      <% end %>
      <% if alert %>
        <div class="alert alert-danger alert-dismissible fade show text-center" role="alert">
          <%= alert %>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
      <% end %>
      
      <%# その他のフラッシュメッセージ %>
      <% flash.each do |message_type, message| %>
        <% next if message_type.to_s == 'notice' || message_type.to_s == 'alert' %>
        
        <% type_class = case message_type.to_s
                          when 'success' then 'success'
                          when 'error' then 'danger'
                          when 'warning' then 'warning'
                          when 'info' then 'info'
                          else 'secondary'
                        end %>
        <div class="alert alert-<%= type_class %> alert-dismissible fade show text-center" role="alert">
          <%= message %>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
      <% end %>
    </div>
    <%# /フラッシュメッセージ表示エリア %>

    <%# ページコンテンツはこれまで通り <main> タグ内に配置 %>
    <main class="container mx-auto mt-5 px-5 flex">
      <%= yield %>
    </main>
    
    <%# Bootstrap JS Bundle %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" 
            xintegrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" 
            crossorigin="anonymous"></script>
            
    <%# === フラッシュメッセージ自動非表示ロジックの追加 === %>
    <script>
      document.addEventListener('DOMContentLoaded', () => {
        // 固定位置にある全ての alert 要素を取得
        const alerts = document.querySelectorAll('.flash-messages-fixed-top .alert');
        
        alerts.forEach(alertEl => {
          // 5秒後にアラートを非表示にする
          setTimeout(() => {
            // BootstrapのAlertコンポーネントのインスタンスを取得
            const bsAlert = new bootstrap.Alert(alertEl);
            // closeメソッドを呼び出し、フェードアウトして要素をDOMから削除
            bsAlert.close();
          }, 5000); // 5000ミリ秒 (5秒)
        });
      });
    </script>
    <%# =================================================== %>
  </body>
</html>