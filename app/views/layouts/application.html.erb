<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Anta-Santa" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# React (esbuild) を動かすためのタグ %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    
    <%# Tailwind CSS CDN %>
    <script src="https://cdn.tailwindcss.com"></script>
    
    <%# Bootstrap等のCSS %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# フラッシュメッセージ固定表示用のCSS %>
    <style>
      .flash-container {
        position: fixed;
        top: 0;
        left: 0;
        right: 0;
        z-index: 50;
        pointer-events: none;
        padding: 1rem;
        display: flex;
        flex-direction: column;
        align-items: center;
      }
      .flash-message {
        pointer-events: auto;
        box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -2px rgba(0, 0, 0, 0.1);
        border-radius: 0.5rem;
      }
    </style>
  </head>

  <body class="<%= content_for(:body_class) %> <%= content_for(:body_class).blank? ? 'bg-amber-100' : '' %>">

    <%# ▼▼▼ 管理者用救済ボタン (ログイン中の管理者のみ表示) ▼▼▼ %>
    <% if user_signed_in? && current_user.email == ENV['ADMIN_EMAIL'] %>
      <div class="fixed top-4 right-4 z-[100]">
        <%= link_to admin_path, class: "flex items-center gap-2 bg-red-600 text-white px-4 py-2 rounded-full shadow-lg hover:bg-red-700 hover:scale-105 transition transform font-bold text-sm border-2 border-white" do %>
          <span class="text-lg">🚑</span>
          <span>救済コード発行</span>
        <% end %>
      </div>
    <% end %>
    <%# ▲▲▲ ここまで ▲▲▲ %>

    <%# フラッシュメッセージエリア %>
    <div class="flash-container">
      <% flash.each do |key, msg| %>
        <% if key.to_sym == :notice || key.to_sym == :alert %>
          <div class="flash-message mt-2 px-6 py-3 text-sm font-medium <%= flash_class(key) %> transition-all duration-300 transform translate-y-0" role="alert">
            <%= msg %>
          </div>
        <% end %>
      <% end %>
    </div>

    <%# メインコンテンツエリア %>
    <main class="py-10">
      <div class="max-w-7xl mx-auto sm:px-6 lg:px-8">
        <%# ▼▼▼ ここにReactのマウント用divが読み込まれる▼▼▼ %>
        <%= yield %>
      </div>
    </main>

    <%# ▼▼▼ サンタのガイドブック表示エリア (JSで描画) ▼▼▼ %>
    <div id="santa-book-portal"></div>

    <script>
      document.addEventListener('turbo:load', () => {
        const alerts = document.querySelectorAll('.flash-container .flash-message');
        alerts.forEach(alertEl => {
          setTimeout(() => {
            alertEl.style.opacity = '0';
            alertEl.style.transform = 'translateY(-10px)';
            alertEl.addEventListener('transitionend', () => alertEl.remove());
          }, 5000);
        });
      });
    </script>
    
  </body>
</html>