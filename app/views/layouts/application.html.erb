<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Anta Santa" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Bootstrap 5 CDN %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" 
          rel="stylesheet" 
          integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" 
          crossorigin="anonymous">
    
    <%# Bootstrap Icons CDN %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <%# フラッシュメッセージ固定表示用のCSS %>
    <style>
      .flash-messages-fixed-top {
        position: fixed;
        top: 20px;
        left: 50%;
        transform: translateX(-50%);
        z-index: 2000;
        width: 90%;
        max-width: 600px;
      }
    </style>
    
  </head>

  <body>
    <%# ナビゲーションバー %>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
      <div class="container-fluid">
        <%= link_to "Anta-Santa", root_path, class: "navbar-brand" %>
        
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav ms-auto">
            <% if user_signed_in? %>
              <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                  <%= current_user.display_name %>
                </a>
                <ul class="dropdown-menu dropdown-menu-end">
                  <li><%= link_to "ダッシュボード", dashboard_path, class: "dropdown-item" %></li>
                  <li><hr class="dropdown-divider"></li>
                  <li><%= button_to "ログアウト", destroy_user_session_path, method: :delete, class: "dropdown-item", style: "text-align: left; border: none; background: none; padding: 0.5rem 1rem; cursor: pointer;" %></li>
                </ul>
              </li>
            <% else %>
              <li class="nav-item">
                <%= link_to "ログイン", new_user_session_path, class: "nav-link" %>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>

    <%# フラッシュメッセージ表示エリア（固定位置） %>
    <div class="flash-messages-fixed-top">
      <% if notice %>
        <div class="alert alert-success alert-dismissible fade show text-center" role="alert">
          <%= notice %>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
      <% end %>
      <% if alert %>
        <div class="alert alert-danger alert-dismissible fade show text-center" role="alert">
          <%= alert %>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
      <% end %>
      
      <%# その他のフラッシュメッセージ %>
      <% flash.each do |message_type, message| %>
        <% next if message_type.to_s == 'notice' || message_type.to_s == 'alert' %>
        
        <% type_class = case message_type.to_s
                          when 'success' then 'success'
                          when 'error' then 'danger'
                          when 'warning' then 'warning'
                          when 'info' then 'info'
                          else 'secondary'
                        end %>
        <div class="alert alert-<%= type_class %> alert-dismissible fade show text-center" role="alert">
          <%= message %>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
      <% end %>
    </div>
    <%# /フラッシュメッセージ表示エリア %>

    <%# ページコンテンツ %>
    <main class="container mx-auto mt-5 px-5 flex">
      <%= yield %>
    </main>
    
    <%# Bootstrap JS Bundle %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" 
            integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" 
            crossorigin="anonymous"></script>
  </body>
</html>