<div class="container mx-auto px-4 py-12 max-w-4xl">
  <h1 class="text-3xl font-extrabold text-gray-800 mb-2 text-center">
    お手紙を送る相手を選択
  </h1>
  <p class="text-center text-gray-500 mb-10">
    あなたの周りの素敵な仲間を選び、暖かいメッセージを送りましょう。
  </p>

  <%# 検索フォーム  %>
  <div class="mb-6">
    <%= form_with url: users_path, method: :get, local: true, class: "flex space-x-2" do %>
      <%= text_field_tag :search, params[:search], 
                         placeholder: "ユーザー名で検索...", 
                         class: "flex-grow px-4 py-3 border border-gray-300 rounded-xl shadow-inner focus:ring-indigo-500 focus:border-indigo-500 transition duration-150" %>
      <%# 検索ボタン: 白背景、枠線、テキスト付き %>
      <%# justify-center を追加し、ボタンの中の文字を中央寄せにする %>
      <%= button_tag type: 'submit', class: "flex items-center justify-center px-4 py-3 bg-white text-gray-700 font-semibold rounded-xl shadow-lg border border-gray-300 hover:bg-gray-50 transition duration-150" do %>
        <i class="fas fa-search  text-lg"></i>検索
      <% end %>
    <% end %>
  </div>

  <%# フレンド一覧へのリンク %>
  <div class="text-center mb-8">
    <%# following_users_path は /users/following に対応 %>
    <%= link_to following_users_path, class: "inline-flex items-center px-4 py-2 bg-green-500 text-white font-semibold rounded-full shadow-md hover:bg-green-600 transition duration-150" do %>
      <i class="fas fa-users mr-2"></i> フォロー中のフレンド一覧を見る
    <% end %>
  </div>
  
  <div class="space-y-4">
    <% if @users.any? %>
      <% @users.each do |user| %>
        <div class="flex items-center justify-between p-4 bg-white rounded-xl shadow-md transition duration-300 hover:shadow-lg">
          <div class="flex items-center">
            <!-- ユーザーアイコンのプレースホルダー -->
            <div class="w-12 h-12 rounded-full bg-indigo-100 flex items-center justify-center text-indigo-600 text-lg font-semibold mr-4">
              <%= user.username.to_s[0].upcase %>
            </div>
            <div>
              <%# ユーザー名をクリックでプロフィールへ遷移 %>
              <%= link_to user.username, user_path(user), class: "text-xl font-bold text-gray-800 hover:text-indigo-600 transition duration-150" %>
            </div>
          </div>
          
          <%# --- ボタンエリア --- %>
          <%# flex-1 を使ってボタンを横並びかつ均等幅にする %>
          <div class="flex space-x-2 w-64 ml-4">
            
            <%# 1. フォローボタン (パーシャル呼び出し) %>
            <div class="flex-1">
              <%= render 'users/follow_form', user: user %>
            </div>
            
            <%# 2. お手紙を送るボタン %>
            <%= link_to 'お手紙を送る', new_evaluation_path(receiver_id: user.id), 
                class: 'flex-1 text-center px-4 py-2 bg-yellow-500 text-white text-sm font-semibold rounded-xl shadow-md hover:bg-yellow-600 transition duration-300' %>
          </div>
          
        </div>
      <% end %>
    <% else %>
      <div class="text-center py-12 bg-white rounded-xl shadow-lg border border-gray-100">
        <svg class="w-12 h-12 text-gray-400 mx-auto mb-4" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z"></path></svg>
        <p class="text-lg font-semibold text-gray-500">
          <% if params[:search].present? %>
            "<%= params[:search] %>" に一致するユーザーは見つかりませんでした。
          <% else %>
            まだ他のユーザーがいません
          <% end %>
        </p>
        <p class="text-sm text-gray-400 mt-1">
          ユーザーが増えたらここに表示されます。
        </p>
      </div>
    <% end %>
  </div>
</div>