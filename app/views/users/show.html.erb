<%# ユーザープロフィール表示画面 %>
<div class="container mx-auto p-4 sm:p-6 lg:p-8">
  <div class="max-w-4xl mx-auto bg-white dark:bg-gray-800 shadow-2xl rounded-xl p-6 md:p-10">

    <div class="flex flex-col md:flex-row items-start md:items-center space-y-6 md:space-y-0 md:space-x-8 border-b pb-6 mb-6">
      
      <%# プロフィール画像と基本情報 %>
      <div class="flex-shrink-0">
        <%# 画像URLがnilの場合のプレースホルダー表示 %>
        <%= image_tag(@user.image_url.presence || "https://placehold.co/100x100/f7f3e8/4a4a4a?text=No+Img", class: "w-24 h-24 rounded-full object-cover border-4 border-red-400 shadow-md", alt: @user.display_name) %>
      </div>

      <div class="flex-grow">
        <h1 class="text-3xl font-extrabold text-gray-900 dark:text-white mb-1">
          <%= @user.display_name %>
        </h1>
        <p class="text-lg text-gray-600 dark:text-gray-400">
          <% if @user.is_admin %>
            <span class="inline-flex items-center px-3 py-0.5 rounded-full text-sm font-medium bg-yellow-100 text-yellow-800">
              管理者
            </span>
          <% end %>
        </p>
      </div>

      <%# フォロー/アンフォローボタン %>
      <%# @user がログインユーザー自身でない場合のみフォームを表示 %>
      <% if current_user %>
        <%= render 'relationships/follow_form' %>
      <% end %>
    </div>

    <%# フォロー数・フォロワー数の表示 %>
    <div class="flex space-x-6 mb-8 text-center border-b pb-6">
      <div class="w-1/2">
        <span class="block text-3xl font-bold text-red-500" id="following_count">
          <%= @user.following.count %>
        </span>
        <span class="block text-sm text-gray-500 dark:text-gray-400">フォロー</span>
      </div>
      <div class="w-1/2">
        <span class="block text-3xl font-bold text-red-500" id="followers_count">
          <%= @user.followers.count %>
        </span>
        <span class="block text-sm text-gray-500 dark:text-gray-400">フォロワー</span>
      </div>
    </div>

    <%# 受け取った評価のサマリー %>
    <h2 class="text-2xl font-bold text-gray-900 dark:text-white mb-4 border-b-2 border-red-500 pb-2">
      <%= @user.display_name %>さんが受け取った評価（直近5件）
    </h2>
    
    <div class="space-y-4">
      <% if @evaluations&.any? %>
        <% @evaluations.each do |evaluation| %>
          <div class="bg-red-50 dark:bg-gray-700 p-4 rounded-lg border-l-4 border-red-500 shadow-sm transition hover:shadow-md">
            <p class="text-gray-800 dark:text-gray-200">
              <%= link_to "メッセージ詳細へ", evaluation_path(evaluation), class: "text-red-600 hover:text-red-700 font-semibold" %>
            </p>
            <p class="text-sm text-gray-500 dark:text-gray-400 mt-1">
              From: <%= evaluation.evaluator.display_name %> / 
              日付: <%= evaluation.created_at.strftime("%Y年%m月%d日") %>
            </p>
          </div>
        <% end %>
      <% else %>
        <p class="text-gray-600 dark:text-gray-400 text-center py-4">まだこのユーザーへの評価はありません。</p>
      <% end %>
    </div>

  </div>
</div>